import java.util.Arrays;import java.util.Scanner;public class LinearSearch {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        System.out.println("enter size of array");        int arlen = sc.nextInt();        int ar [] = new int[arlen];        System.out.println("enter array values");        for (int i = 0; i < arlen; i++) {            ar[i] = sc.nextInt();        }        System.out.println("Enter the search element");        int se = sc.nextInt();        LinearSearch ls = new LinearSearch();        ls.linearsearch(ar,se);    }    public void linearsearch(int[] ar,int se)    {        int count =0;        for (int i=0; i<ar.length; i++)        {            if (ar[i] == se)            {//search the elements through the array and display the position                System.out.println("Element " + ar[i]+ " is found at " + " array position "+i);                return;            }        }//display if not found in the array        System.out.println("Element is not in the array");        return;    }}